node_modules/
dist/

# Electron build artifacts (generated by `npm run icons` / `npm run dist:mac`)
release/
build/icon.png
electron/trayTemplate*.png

# Local runtime config & secrets
.env
.env.*
!.env.example
config.json

# Keys / credentials
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json

# Editor / OS
.DS_Store
*.log

# Internal tooling / process artifacts (not for publication)
.superpowers/
.claude/
audit/
docs/superpowers/
